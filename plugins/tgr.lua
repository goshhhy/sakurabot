--[[
This plugin is dead simple.
Responds to a specific message from a specific user, with a specific response.
It can be set up as a oneshot, so it doesn't repeatedly respond.
]]

return {
    name = "targetresponse",
    version = "0.1",
}